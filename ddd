def cmmdc(m,n):
    while m!=n:
        if m>n: m = m - n
        else: n = n - m
    return m
m = int(input('m = '))
n = int(input('n = '))
print('cel mai mare divizor comun=', cmmdc(m,n))
print('cel mai mic multiplu comun =', str(m*n // cmmdc(m,n)))
